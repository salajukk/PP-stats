function showError(msg){document.getElementById('loading').style.display='none';const el=document.getElementById('error-msg');el.style.display='block';el.querySelector('.tech').textContent=msg}
function updateRecord(){const w=tGames.filter(g=>g[COL.tulos]==='Voitto').length,l=tGames.filter(g=>g[COL.tulos]==='Tappio').length;document.getElementById('record').textContent=`${w}–${l}`}
function showTab(id,btn){document.querySelectorAll('.section').forEach(s=>s.classList.remove('active'));document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));document.getElementById(id).classList.add('active');btn.classList.add('active');document.getElementById('seasonSel').classList.toggle('history-hidden',id==='playerHistory'||id==='teamHistory')}
function changeSeason(){curSeason=document.getElementById('seasonSel').value;selectedSeasons=new Set([curSeason]);processSeason(curSeason);r1=players[0]||'';r2=players[1]||players[0]||'';tlSel=new Set(players.slice(0,3));activePlayer=null;activeOpp=null;renderAll()}
function renderAll(){updateRecord();buildRadarChips();buildRadar();buildTlChips();buildTimeline();buildTeamChart();buildShooting();renderBothTables();renderPlayerHistory();renderTeamHistory();document.getElementById('upd').textContent=`Päivitetty ${new Date().toLocaleString('fi-FI',{dateStyle:'short',timeStyle:'short'})}`}
async function initApp(){try{await loadData();const sel=document.getElementById('seasonSel');sel.innerHTML='';seasons.forEach(s=>{const o=document.createElement('option');o.value=s;o.textContent=s;sel.appendChild(o)});selectedSeasons=new Set([curSeason]);r1=players[0]||'';r2=players[1]||players[0]||'';tlSel=new Set(players.slice(0,3));renderAll();document.getElementById('loading').style.display='none';document.getElementById('app').style.display='block'}catch(e){showError(e.message)}}
initApp();
